aws_region                   = "# REQUIRED — set in your local untracked terraform.tfvars"
eks_cluster_name             = "# REQUIRED — set in your local untracked terraform.tfvars"
control_plane_namespace      = "# REQUIRED — set in your local untracked terraform.tfvars"
root_domain                  = "# REQUIRED — set in your local untracked terraform.tfvars"
cloudflare_zone_id           = "# REQUIRED — set in your local untracked terraform.tfvars"
control_plane_ingress_hostname = "# REQUIRED — set in your local untracked terraform.tfvars"
instance_ingress_hostname      = "# REQUIRED — set in your local untracked terraform.tfvars"
cloudflare_api_token           = "# REQUIRED — set in your local untracked terraform.tfvars"
cors_origins                 = "# REQUIRED — set in your local untracked terraform.tfvars"
manage_dns                  = "# REQUIRED — set in your local untracked terraform.tfvars"
db_host                      = "# REQUIRED — set in your local untracked terraform.tfvars"
db_port                      = "# REQUIRED — set in your local untracked terraform.tfvars"
db_name                      = "# REQUIRED — set in your local untracked terraform.tfvars"

db_password = "# REQUIRED — set in your local untracked terraform.tfvars"

db_admin_password = "# REQUIRED — set in your local untracked terraform.tfvars"
db_admin_sslmode             = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_host                   = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_port                   = "# REQUIRED — set in your local untracked terraform.tfvars"
container_registry_host      = "# REQUIRED — set in your local untracked terraform.tfvars"
control_plane_image_tag      = "# REQUIRED — set in your local untracked terraform.tfvars"
control_plane_web_image_tag  = "# REQUIRED — set in your local untracked terraform.tfvars"
control_plane_release_id     = "# REQUIRED — set in your local untracked terraform.tfvars"
default_platform_release_id  = "# REQUIRED — set in your local untracked terraform.tfvars"
cert_manager_issuer          = "# REQUIRED — set in your local untracked terraform.tfvars"
enable_billing               = "# REQUIRED — set in your local untracked terraform.tfvars"
s3_bucket_pack_artifacts     = "# REQUIRED — set in your local untracked terraform.tfvars"
instance_terraform_state_bucket = "# REQUIRED — set in your local untracked terraform.tfvars"
instance_terraform_state_region = "# REQUIRED — set in your local untracked terraform.tfvars"
instance_terraform_lock_table   = "# REQUIRED — set in your local untracked terraform.tfvars"
instance_terraform_state_prefix = "# REQUIRED — set in your local untracked terraform.tfvars"
db_instance_identifier       = "# REQUIRED — set in your local untracked terraform.tfvars"
db_subnet_group_name         = "# REQUIRED — set in your local untracked terraform.tfvars"
db_subnet_ids                = "# REQUIRED — set in your local untracked terraform.tfvars"
db_security_group_ids        = "# REQUIRED — set in your local untracked terraform.tfvars"
db_parameter_group_name      = "# REQUIRED — set in your local untracked terraform.tfvars"
db_kms_key_id                = "# REQUIRED — set in your local untracked terraform.tfvars"
db_monitoring_role_arn       = "# REQUIRED — set in your local untracked terraform.tfvars"
db_performance_insights_kms_key_id = "# REQUIRED — set in your local untracked terraform.tfvars"
db_backup_window             = "# REQUIRED — set in your local untracked terraform.tfvars"
db_maintenance_window        = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_replication_group_id   = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_replication_group_description = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_node_type              = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_engine_version         = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_subnet_group_name      = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_subnet_ids             = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_security_group_ids     = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_parameter_group_name   = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_snapshot_retention_limit = "# REQUIRED — set in your local untracked terraform.tfvars"
redis_snapshot_window        = "# REQUIRED — set in your local untracked terraform.tfvars"
ecr_control_plane_repository = "# REQUIRED — set in your local untracked terraform.tfvars"
ecr_web_control_plane_repository = "# REQUIRED — set in your local untracked terraform.tfvars"
