# W0 Baseline — 2026-05-09

Captured at HEAD `96c92c2` of `claude/condescending-shamir-92422b`.

This is the ground-truth measurement before W0 work begins. **Significant reconciliation against the 2026-05-08 senior-architect audit:** several audit findings are out-of-date because Plan Fix 1 (commit `e575a76`, 2026-05-04) shipped before the audit, but the reviewer agent reported state that does not exist in the working tree. The reconciliation table below revises the master roadmap.

## Scanner Results

| Scanner | Exit Code | Output Summary | Reconciled Findings |
|---|---|---|---|
| `compliance:check` | **1 (BROKEN)** | `TSError: Cannot find module 'glob'` at `tools/design-compliance/terminology-scanner.ts:20` | **NEW: F151** — scanner does not compile; CI must currently be skipping it or accepting failure |
| `authz:check` | 0 | `authz bypass check: ok` | Confirms **F018** — `SERVICE_ROOT` at line 10 is `apps/svc-data/src/app` only |
| `audit:check` | 0 | `audit bypass check: ok`; KNOWN_DEFERRED_OFFENDERS empty | Scanner narrow per **F044**; v2 still needed in W5 |
| `security:check` | 0 | `security bypass check: ok`; PUBLIC_ALLOWLIST = 11 entries | **F105 confirmed** — drift = 28 actual @Public sites vs 11 allowlisted; **but scanner exits 0** so it doesn't enforce drift, only banned-pattern presence |
| `service-boundary:check` | 0 | `4 entity ownership rule(s) enforced` (AutomationRule + 3 related); `7 allowlisted entity crossing(s)` | **F056 partially wrong** — entity-import rule exists for AutomationRule. Audit reviewer missed it. **But the rule blocks IMPORTS, not WRITES via injected interfaces.** Net: F056 reduces to "extend rule to detect repository-write patterns" — narrower than master roadmap claimed |
| `deps:check` | 0 | `1 legacy carve-out(s) tracked` (bcrypt only) | **F107 partially resolved** — node-cache and @hello-pangea/dnd already removed. Only bcrypt remains as legacy |
| `lint` | n/a | `'nx' is not recognized` (no node_modules in worktree) | Pending `npm install` — re-run before W0 acceptance |
| `test` | n/a | Same — pending node_modules | — |

## Audit Reconciliation Table

Findings that the master roadmap (`00-master-remediation-roadmap.md`) needs to revise based on actual state:

| Finding | Master Roadmap Claim | Actual State (2026-05-09) | Disposition |
|---|---|---|---|
| **F055** | "svc-data still owns full automation runtime; 17 files including `automation.service.ts` (568 lines), `automation-executor.service.ts` (465 lines), `scheduler.service.ts`, `ava-automation.service.ts`, etc." | `apps/svc-data/src/app/automation/` contains exactly **2 files**: `sync-trigger-client.service.ts` + `.spec.ts`. Plan Fix 1 (`e575a76`, 2026-05-04) did the consolidation correctly. | **RESOLVED-PRE-W0.** Audit reviewer was wrong; the runtime is gone. Drop F055 from W4. |
| **F056** | "service-boundary scanner has NO rule about AutomationRule writes" | Scanner has `ENTITY_OWNERSHIP` map at `tools/service-boundary-check.ts:64-69` enforcing AutomationRule + 3 related entities. Allowlist of 7 design-time crossings. | **PARTIALLY RESOLVED.** Rule exists but enforces by *import topology* not by `.save/.update/.delete` calls on injected `Repository<X>` interfaces. Narrowed F056 = "extend rule to detect repository-write patterns where the entity is received via DI, not imported." Still W0 task 5 work, but smaller scope than originally written. |
| **F058** | "data.service.ts CRUD path bypasses ALL automation, validation, defaults, outbox" | Need to verify — `apps/svc-data/src/app/data.service.ts` may have been removed or refactored as part of Plan Fix 1. | **PENDING VERIFICATION** in next session. |
| **F059** | "Recursion guard wrong — allows infinite same-record loops (svc-data only)" | If F055 is resolved, the cited code at `automation-executor.service.ts:102` no longer exists. | **LIKELY-RESOLVED** with F055. |
| **F071** | "AVA proposal guard exists in svc-automation, NOT in svc-data's AVA path" | If F055 resolved, `apps/svc-data/src/app/automation/automation.controller.ts:289-307` no longer exists. | **LIKELY-RESOLVED** with F055. |
| **F107** | "Banned dependencies still installed (bcrypt, node-cache, @hello-pangea/dnd)" | Only **bcrypt** remains in `package.json:84` and as legacy carve-out in `tools/approved-deps.json`. node-cache and @hello-pangea/dnd already gone. | **PARTIALLY RESOLVED.** F107 narrows to "remove bcrypt" only. |
| **F104** | "eslint.config.mjs enforces almost nothing the canon claims" | **Confirmed.** Config is 50 lines, only `@nx/enforce-module-boundaries` + `no-console: warn`. None of the 5 claimed §21 rules present. | **STILL OPEN.** W0 task 6 proceeds as written. |
| **F105** | "security-bypass-check.ts PUBLIC_ALLOWLIST incomplete (CI must currently be failing or not running)" | Allowlist has 11 entries; **`grep -rln "@Public()" apps/ libs/` returns 28 sites.** Drift = 17. **But the scanner exits 0** because it's not actually checking allowlist compliance — it scans for banned PATTERNS (eval, new Function, spawn) with their own per-pattern allowlists, NOT for unallowlisted @Public() sites. F105 is a different kind of bug: PUBLIC_ALLOWLIST is *ornamental* — it documents what the team considers acceptable but isn't *enforced by the scanner*. | **STILL OPEN, scope refined.** W0 task 3 must add the drift check (asserting every `@Public()` site is in allowlist) and reconcile contents. |
| **F106** | "CD workflow disables CI's gates with if: always() / no dep on CI" | **Confirmed.** `.github/workflows/cd.yml` triggers on `push: branches: [main, develop]` independent of CI. | **STILL OPEN.** W0 task 7 proceeds as written. |
| **F018** | "tools/authz-bypass-check.ts only scans svc-data" | **Confirmed.** Line 10: `const SERVICE_ROOT = join(APPS_ROOT, 'svc-data', 'src', 'app')`. | **STILL OPEN.** W0 task 4 proceeds as written. |
| **F119** | "No SBOM, license-scan, or secret-scan in CI" | Need to verify against `.github/workflows/ci.yml`. Tentative confirmation. | **STILL OPEN.** W0 tasks 8-9 proceed. |
| **F121** | "AVA AST fallback parser tests carry TODOs" | Need verification — files cited at `script-sandbox.service.spec.ts:186, 219`. | **PENDING VERIFICATION** when ESLint runs. |

## New Findings Discovered During Baseline

| ID | Finding | Source | Owning Wave |
|---|---|---|---|
| **F151** | `tools/design-compliance/terminology-scanner.ts:20` imports `glob` which is not in `package.json`. The scanner does not compile. CI is either silently skipping it or accepting non-zero exit, in violation of the canon's §21 enforcement claim. | Baseline scanner run | **W0** (must fix as part of Task 1.5 — added inline) |

## Discoveries About the Audit Itself

- **Audit reviewer agent for "automation/workflow runtime" produced demonstrably false findings about svc-data automation.** The reviewer cited file paths and line ranges that do not exist in the tree at the audit's stated commit. Possible causes: stale grep cache, wrong worktree, or fabrication. **This means the audit is not 100% trustworthy as written — every finding requires verification before being treated as actionable.**
- **Conversely, the reviewer for "data plane" appears to have been accurate** — the core data-plane findings (F042, F043, F044, F046-F050) refer to real code patterns and will need verification but are likely true.
- **The audit's value is the *category* of findings, not every individual file:line.** Every finding ID should be re-grounded against current state before its owning wave executes.

## Master Roadmap Revisions Required

1. Add **F151** to Inventory under W0.
2. Mark **F055** as RESOLVED-PRE-W0 in Inventory and remove from W4.
3. Mark **F107** as PARTIALLY-RESOLVED; W4 scope narrows to bcrypt only.
4. Mark **F056** as PARTIALLY-RESOLVED; W0 task 5 scope narrows to repository-write detection (not import-topology, which already exists).
5. Mark **F059, F071** as LIKELY-RESOLVED-WITH-F055; verify in next session before W4 starts.
6. Mark **F058** as PENDING-VERIFICATION; verify `data.service.ts` state before W4.
7. Add a per-finding "verification before remediation" step to every wave's detailed plan: each wave's first task is to re-ground its findings against current state.

## Constraints on This Worktree

- `node_modules` not yet installed — `npm install` running in background. Lint, tests, jest-based scanner self-tests pending.
- No GitHub admin access from this session — Task 11 (required-status-checks config) requires human admin.

## Next Step

Continue W0 with revised scope:
- Task 1.5 (NEW): fix F151 (broken `compliance:check` — add `glob` dep).
- Task 2: scanner self-test framework (proceeds as written).
- Task 3: PUBLIC_ALLOWLIST enforcement upgrade (scope refined per F105 reconciliation).
- Task 4: authz scanner scope (proceeds as written).
- Task 5: service-boundary entity-write detection (scope narrowed).
- Tasks 6-12: proceed as written.

Each task's first sub-step: re-verify the finding against current code before applying the fix.
